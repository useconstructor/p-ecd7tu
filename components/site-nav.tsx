'use client'

import { useState } from "react";
import { LayoutGrid, Menu, X } from "lucide-react";

const links=[["Product","#product"],["Features","#features"],["Pricing","#pricing"],["Customers","#customers"]];
export function SiteNav(){const[open,setOpen]=useState(false);return <nav className="fixed inset-x-0 top-0 z-50 border-b border-line/80 bg-white/90 backdrop-blur-xl"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-5 lg:px-8"><a href="#product" className="flex items-center gap-2 text-sm font-bold"><span className="grid h-7 w-7 place-items-center rounded-md bg-blue text-white"><LayoutGrid size={15}/></span>Flowboard</a><div className="hidden items-center gap-7 md:flex">{links.map(([l,h])=><a key={l} href={h} className="text-xs font-medium text-slate transition hover:text-ink">{l}</a>)}</div><a href="#workspace" className="hidden rounded-md bg-ink px-4 py-2 text-xs font-semibold text-white md:block">Open workspace</a><button onClick={()=>setOpen(v=>!v)} aria-label="Toggle navigation" className="md:hidden">{open?<X/>:<Menu/>}</button></div><div className={`absolute inset-x-0 top-16 border-b border-line bg-white p-5 transition-all duration-500 ease-[cubic-bezier(0.16,1,0.3,1)] md:hidden ${open?'opacity-100 translate-y-0 pointer-events-auto':'opacity-0 -translate-y-4 pointer-events-none'}`}>{links.map(([l,h],i)=><a key={l} href={h} onClick={()=>setOpen(false)} style={{transitionDelay:open?`${i*60}ms`:'0ms'}} className="block border-b border-line py-4 text-sm font-semibold transition-all">{l}</a>)}<a href="#workspace" onClick={()=>setOpen(false)} className="mt-4 block rounded-md bg-blue p-3 text-center text-sm font-semibold text-white">Open workspace</a></div></nav>}
